import { idGenerator } from "../utils/uid.js";

export async function transferTobalanceAfterPurchasingCamaign(id, amount) {
  // ... retrieve any user bank data needed.
  // ... add the stripe balance and die function.
  return idGenerator(20);
}
